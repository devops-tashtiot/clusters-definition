# Environment-specific overrides for minio.
# Merged on top of clusters-provision/clusters/minio/values.yaml by ArgoCD.

# Same shared platform-wide admin password every other devtool/cluster-infra
# tool uses (rhbk, bitbucket, artifactory, ...) — never a per-tool parameter.
adminPasswordSsmParameter: "/devtools/admin/password"
